<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <title></title>
</head>
<body>
    <div>
        @if (TempData["savedMsg"] != null)
        {
            <div class="alert alert-success">@TempData["savedMsg"]</div>
        }
        @RenderBody()
    </div>
</body>
</html>
